spring:
  application:
    name: medical
  config:
    import: ${SPRING_CONFIG_IMPORT:optional:configserver:http://localhost:8071}
  profiles:
    active: dev
  redis:
    host: ${SPRING_REDIS_HOST:localhost}
    port: ${SPRING_REDIS_PORT:6379}
  datasource:
    enabled: true
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:db/changelog/liquibase-master.xml
  zipkin:
    baseUrl: ${SPRING_ZIPKIN_BASE_URL:http://localhost:9411/api/v2/spans}
server:
  port: 9003


vnpay:
  url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  return-url: ${SPRING_VNPAY_URL:http://localhost:5173/payment/result}
  tmn-code: PVV29YGZ
  secret-key: MC0D3E2FLJTXVVQTWB0A2WDF3IG59VX8
  version: 2.1.0
  command: pay
  order-type: other
